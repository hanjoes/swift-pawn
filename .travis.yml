matrix:
  include:
    - os: linux
      dist: trusty
    - os: osx
      osx_image: xcode10.2

language: swift
sudo: required
env: SWIFT_VERSION=5.0.1
install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - swift build